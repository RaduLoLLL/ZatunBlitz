import { Ping } from "@uiball/loaders"

const Loader = () => {
  return (
    <div className="min-h-screen flex justify-center items-center">
      <Ping size={45} speed={2} color="#DEB52D" />
    </div>
  )
}
export default Loader
